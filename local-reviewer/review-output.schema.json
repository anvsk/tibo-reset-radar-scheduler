{
  "type": "object",
  "additionalProperties": false,
  "required": ["results"],
  "properties": {
    "results": {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "verdict",
          "reasonKey",
          "confidence",
          "summaryZh",
          "reason"
        ],
        "properties": {
          "id": { "type": "string" },
          "verdict": {
            "type": "string",
            "enum": ["confirmed", "strong_hint", "negative", "irrelevant"]
          },
          "reasonKey": {
            "type": "string",
            "enum": ["direct_reset", "banked_reset", "reset_plan", "no_reset", "irrelevant"]
          },
          "confidence": { "type": "integer", "minimum": 0, "maximum": 100 },
          "summaryZh": { "type": "string" },
          "reason": { "type": "string" }
        }
      }
    }
  }
}
